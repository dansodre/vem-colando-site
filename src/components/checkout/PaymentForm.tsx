const PaymentForm = () => (
  <div>
    <p className="text-muted-foreground">O formulário de pagamento seguro do Stripe será carregado aqui.</p>
    {/* O Stripe Elements será injetado aqui posteriormente */}
  </div>
);

export default PaymentForm;
